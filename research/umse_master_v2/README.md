# UMSE MASTER V2 FULL

UMSE V2 FULL is the next-generation research track built **on top of the repaired UMSE V1 research baseline**. It is isolated from production CLEAR NASDAQ and does not modify BASE_FIA.

Parent branch head at V2 fork:

`09d10c91ed246c887cd8cd5cac5a4aa47b08a5da`

Parent repaired V1 scientific freeze:

`c58adb7a4aad23c22bd745ab1494a8d87692aeeb`

## V2 objective

V2 asks a harder question than V1:

> Can genuine microstructure identity, queue survival, order-book memory, market resistance, information propagation, causal mechanism competition and cross-scale state survival add reproducible incremental information beyond repaired UMSE V1 and BASE_FIA?

V2 is not a new indicator collection. It is a research architecture for testing deeper market-mechanism hypotheses without promoting unavailable data into fake precision.

## First build block

The initial V2 block is the **microstructure truth layer**:

- genuine MBO order-lifecycle contracts;
- exact queue-survival eligibility;
- order lineage and sequence integrity;
- explicit separation of order identity from trader identity;
- descriptive order-book memory;
- an uncalibrated market-resistance field;
- fail-closed behavior when lifecycle history is incomplete.

Later V2 blocks will add causal information velocity, latent game/agent competition, structural/topological regime diagnostics, counterfactual mechanism tests, renormalized cross-scale state transport, MDL/Bayesian model competition and a V2-specific paired validation layer.

## Non-negotiable truth boundaries

- L2 is never called MBO.
- An order ID is not a trader ID.
- Missing lifecycle events invalidate exact queue claims.
- Sequence gaps are explicit degradation, never imputed as intact history.
- Synthetic/proxy rows can test software but cannot prove predictive value.
- No V2 module may change production outputs.
- No engineering test proves market edge.
- No predictive promotion before unseen forward evidence.

Current scientific flags remain:

`SUCCESSOR_CAMPAIGN_STARTED = false`

`ALPHA_SPENT = 0`

`PREDICTIVE_EDGE = NOT_PROVEN`

`PRODUCTION_INTEGRATION = false`
