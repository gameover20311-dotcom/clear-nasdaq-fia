uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.9
python-dotenv>=1.0
python-multipart>=0.0.18
yfinance>=0.2.65
google-genai>=1.0

# A2Z HARDENING — public production runtime.
# FastAPI 0.128.x constrained Starlette to the pre-1.x line. Starlette 0.49.3
# is affected by 2026 security advisories. Production is pinned to Python 3.12
# via backend/.python-version and deliberately uses the patched Starlette 1.x
# generation. CI installs these exact constraints and imports/builds the API.
fastapi==0.141.1
starlette>=1.3.1,<2

# V6.6.8 DURABLE AUTH STORAGE — Psycopg 3.
# Production Python is pinned >=3.10, so use the wheel-backed 3.3 line.
psycopg[binary]>=3.3.5,<4
