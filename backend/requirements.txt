uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.8
python-dotenv>=1.0
python-multipart>=0.0.9
yfinance>=0.2.65
google-genai>=1.0
fastapi==0.128.8
starlette==0.49.3

# V6.6.8: durable auth + Forward-OOS storage when DATABASE_URL is configured.
# Absent DATABASE_URL the code falls back to SQLite and reports EPHEMERAL.
psycopg[binary]==3.2.3
