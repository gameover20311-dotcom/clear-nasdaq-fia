# V6 CAUSAL-TWIN ARCHITECTURE

/api/dashboard (ONE atomic truth)
        |
        v
Evidence ledger -> Evidence Genome -> Regime Novelty Gate
        |
        +-> 4 specialists
        +-> validated causal graph -> Deterministic Market Twin
                                   -> contradiction/concentration cap
        +-> Competing Hypothesis Ledger
        +-> BULL / BASE / BEAR Scenario Lattice -> entropy cap
        |
        v
PRECOMMITMENT HASH (before final answer)
        |
        +-> 3 forecasters + bull/bear stress cases
        +-> skeptic + three-role tribunal
        +-> temporally-safe Failure Memory cap
        v
Chief FIA -> invariants -> deterministic confidence caps -> SHADOW output
        |
        v
Future outcome -> immutable autopsy -> Failure Memory (eligible only for later timestamps)

Research proof lane:
TRAIN = calibration/regime fitting | DEV = iteration + ablation | HOLDOUT = sealed blind parity | Forward-OOS = market evidence
