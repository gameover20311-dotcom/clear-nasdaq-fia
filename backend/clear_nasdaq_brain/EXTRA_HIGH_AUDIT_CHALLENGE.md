# GPT-5.6 SOL EXTRA HIGH — BREAK V6

Try to prove one of these failures reproducibly:
1. provider-health enters LLM evidence;
2. multiple live snapshots are joined into synthetic truth;
3. evidence-genome hash can remain valid after evidence mutation;
4. market twin invents a causal driver not present in validated chains;
5. high causal contradiction fails to cap confidence;
6. scenario probabilities do not sum to 100 or duplicate a world;
7. high scenario entropy fails to cap confidence;
8. Chief rewrites hypotheses after precommitment hash;
9. regime profile trains on DEV/HOLDOUT;
10. regime novelty creates direction instead of only limiting confidence;
11. failure-memory uses an outcome resolved after the forecast timestamp;
12. duplicate autopsy/outcome overwrites history;
13. DEV ablation tool accepts HOLDOUT;
14. HOLDOUT output/reference overwrite is possible through normal scripts;
15. full reasoning/code seal ignores any V6 reasoning module;
16. invalid evidence IDs survive any V6 structured layer;
17. local model malformed output escapes fail-closed;
18. concurrent inference bypasses single-flight memory guard;
19. sidecar binds externally;
20. any paid/remote API path exists.

PASS = no reproducible known critical/high defect in the tested scope. It never means “zero unknown bugs forever.”
